/**
 * Valid section values for Insights Subcategories
 */
export enum InsightsSection {
  CAUSE = '발생원인',
  RISK = '리스크',
  CHECKPOINT = '체크포인트',
  SOLUTION = '함께 실행방안',
  CASE = '케이스',
}

/**
 * Array of all valid section values
 */
export const VALID_INSIGHTS_SECTIONS = Object.values(InsightsSection);

